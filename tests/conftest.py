"""Pytest configuration.

Currently empty — the package is installed via ``pip install -e .[test]``
so its imports resolve from the editable install, no sys.path mutation
needed. Kept as a discovery anchor for pytest and a place to add
session-scoped fixtures if the suite grows them.
"""
